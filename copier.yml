project_name:
  type: str
  help: What is the Django project name? (Should be a valid Python package name)

include_trojsten:
  type: bool
  help: Include Trojsten specific stuff (CSS theme, OIDC setup)
